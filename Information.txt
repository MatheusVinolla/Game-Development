LOVE2D FUNCTIONS:

GRAPHICS()
- love.graphics.printf(msg, x, y, [width], [align])
- love.graphics.print(msg, x, y)

- love.graphics.setDefaultFilter('nearest', 'nearest')
- love.graphics.rectangle(['fill'/'line'], x, y, [width], [tall])
- love.graphics.clear(r, g, b, a)

- object_font = love.graphics.newFont(path, size)
- love.graphics.setFont(font)
 
RANDOM()
- love.window.setMode(REAL_WIDTH, REAL_HEIGHT, { resizable = false, vsync = true, fullscreen = false})
- love.event.quit()
 
DEFINITION:
- love.keypressed(key)
- love.load()
- love.draw()
 
PUSH:
- push.start()
- push.finish()
- push.setupScreen(VIRTUAL_WIDTH, VIRTUAL_HEIGHT, { upscale = 'normal'})
